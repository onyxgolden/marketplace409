{"$schema":"../gen/schemas/desktop-schema.json","description":"Minimal FORGE Capture permissions: core windowing for the main window and the region-picker overlay. No network, no broad filesystem access — all file writes happen in the backend under the app's own captures directory.","identifier":"default","permissions":["core:default","core:window:allow-close","core:window:allow-set-size"],"windows":["main","overlay"]}