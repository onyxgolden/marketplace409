import CaptureEditorHost from "@/components/forge/capture/CaptureEditorHost";

export const metadata = {
  title: "Capture | FORGE",
  description: "Screenshot and image markup with pixel-baked redaction export.",
};

export default function CapturePage() {
  return <CaptureEditorHost />;
}
