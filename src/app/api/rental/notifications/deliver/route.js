import{NextResponse}from"next/server";import{createRentalWebhookClient}from"@/lib/supabase/createRentalWebhookClient";import{createHttpRentalEmailProvider}from"@/infrastructure/notifications/HttpRentalEmailProvider";
export const runtime="nodejs";export async function POST(request){if(!process.env.RENTAL_NOTIFICATION_DELIVERY_SECRET||request.headers.get("authorization")!==`Bearer ${process.env.RENTAL_NOTIFICATION_DELIVERY_SECRET}`)return NextResponse.json({error:"Unauthorized."},{status:401});try{const db=createRentalWebhookClient(),claim=await db.rpc("claim_rental_email_notification");if(claim.error)throw claim.error;if(!claim.data?.id)return NextResponse.json({success:true,delivered:false});const n=claim.data,settings=await db.from("rental_email_settings").select("sender_name,sender_email,status").eq("owner_id",n.owner_id).eq("status","active").single();if(settings.error)throw settings.error;try{const sent=await createHttpRentalEmailProvider().send({id:n.id,ownerId:n.owner_id,senderName:settings.data.sender_name,senderEmail:settings.data.sender_email,recipient:n.recipient,subject:n.subject,bodyText:n.body_text});const done=await db.rpc("complete_rental_email_delivery",{p_owner_id:n.owner_id,p_notification_id:n.id,p_succeeded:true,p_provider_message_id:sent.messageId,p_failure_message:null});if(done.error)throw done.error;return NextResponse.json({success:true,delivered:true,notificationId:n.id});}catch(error){await db.rpc("complete_rental_email_delivery",{p_owner_id:n.owner_id,p_notification_id:n.id,p_succeeded:false,p_provider_message_id:null,p_failure_message:error instanceof Error?error.message:"Provider delivery failed."});return NextResponse.json({error:"Email delivery failed.",notificationId:n.id},{status:502})}}catch(error){console.error("Rental email delivery error",error);return NextResponse.json({error:"Unable to process rental email delivery."},{status:500})}}
