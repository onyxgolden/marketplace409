import{NextResponse}from"next/server";import{createRentalWebhookClient}from"@/lib/supabase/createRentalWebhookClient";import{executeAutopayAttempt}from"@/application/rental/executeAutopayAttempt";
export const runtime="nodejs";export async function POST(request){if(request.headers.get("authorization")!==`Bearer ${process.env.RENTAL_AUTOPAY_EXECUTION_SECRET}`||!process.env.RENTAL_AUTOPAY_EXECUTION_SECRET)return NextResponse.json({error:"Unauthorized."},{status:401});try{const{enrollmentId,chargeId}=await request.json();const result=await executeAutopayAttempt(createRentalWebhookClient(),enrollmentId,chargeId);return NextResponse.json(result.body,{status:result.httpStatus});}catch(error){console.error("Rental autopay execution error",error);return NextResponse.json({error:"Unable to execute autopay."},{status:500});}}
