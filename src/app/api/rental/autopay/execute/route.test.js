import{describe,expect,it,vi}from"vitest";vi.mock("@/lib/supabase/createRentalWebhookClient",()=>({createRentalWebhookClient:vi.fn()}));vi.mock("@/infrastructure/billing/StripeBillingProvider",()=>({createStripeBillingProvider:vi.fn()}));import{POST}from"./route.js";describe("autopay execution",()=>{it("rejects callers without the execution secret",async()=>{process.env.RENTAL_AUTOPAY_EXECUTION_SECRET="secret";const r=await POST(new Request("https://test/api",{method:"POST",body:"{}"}));expect(r.status).toBe(401)});});
