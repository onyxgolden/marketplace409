import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";const sql=readFileSync("supabase/migrations/20260813003000_reconcile_rental_payment_reversals.sql","utf8");describe("rental reversals",()=>{it("recomputes charge balance from valid payment value",()=>{expect(sql).toContain("status='partially_refunded' then amount_cents-refunded_amount_cents");expect(sql).toContain("update rent_charges set paid_amount_cents=v_paid")});it("posts a separate negative financial adjustment idempotently",()=>{expect(sql).toContain("'forge_rental_payment_adjustment'");expect(sql).toContain("-v_adjustment/100.0");expect(sql).toContain("on conflict(owner_id,source_system,source_record_id) do update")});it("preserves original payment history",()=>expect(sql).not.toMatch(/delete\s+from\s+rental_payments/i));});
