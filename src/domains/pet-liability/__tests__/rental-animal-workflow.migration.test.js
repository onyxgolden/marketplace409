import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";const sql=readFileSync("supabase/migrations/20260813003300_add_rental_animal_workflow.sql","utf8");describe("animal workflow",()=>{it("allows tenant pet or assistance review requests",()=>{expect(sql).toContain("request_rental_animal");expect(sql).toContain("('pet','assistance_review_requested')")});it("requires human assistance classification",()=>expect(sql).toContain("Assistance request requires a human classification decision"));it("prohibits fees for every non-pet classification",()=>{expect(sql).toContain("if classification<>'pet'");expect(sql).toContain("Monthly pet fees are prohibited")});});
