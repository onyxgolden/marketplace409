const BASE_URL="https://secure.rentecdirect.com/api/v3",cents=value=>Math.round(Number(value||0)*100);
export class RentecApiClient{constructor({apiKey,fetchImpl=fetch,baseUrl=BASE_URL}={}){if(!apiKey)throw new Error("Rentec API key is not configured.");this.apiKey=apiKey;this.fetchImpl=fetchImpl;this.baseUrl=baseUrl;}async get(path,parameters={}){const url=new URL(`${this.baseUrl}${path}`);for(const[key,value]of Object.entries(parameters))if(value!==null&&value!==undefined&&value!=="")url.searchParams.set(key,String(value));const response=await this.fetchImpl(url,{headers:{"X-API-Key":this.apiKey,Accept:"application/json","User-Agent":"FORGE-Rental-Migration/1.0"},cache:"no-store"});if(!response.ok)throw new Error(`Rentec API request failed with HTTP ${response.status}.`);return response.json();}async inventory(){const[properties,tenants,leases]=await Promise.all([this.get("/properties",{archived:true}),this.get("/tenants",{archived:true}),this.get("/leases",{start_date:"1900-01-01"})]),propertyRows=properties.data||[],tenantRows=tenants.data||[],leaseRows=leases.data||[];return Object.freeze({propertyIds:Object.freeze(propertyRows.map(row=>String(row.property_id||row.id)).filter(Boolean)),tenantIds:Object.freeze(tenantRows.map(row=>String(row.renter_id||row.id)).filter(Boolean)),leaseIds:Object.freeze(leaseRows.map(row=>String(row.lease_id||row.id)).filter(Boolean)),properties:propertyRows.length,tenants:tenantRows.length,archivedTenants:tenantRows.filter(row=>row.archived===true||row.archived===1).length,leases:leaseRows.length});}async transactionPage({propertyId,page=1}){if(!/^\d+$/.test(String(propertyId))||!Number.isInteger(page)||page<1)throw new Error("A valid Rentec property and page are required.");const payload=await this.get("/transactions",{property_id:propertyId,start_date:"1900-01-01",page}),rows=payload.data||[],byRenter={};let unassigned=0,totalCents=0;for(const row of rows){totalCents+=cents(row.amount);const renterId=row.renter_id?String(row.renter_id):"";if(renterId)byRenter[renterId]=(byRenter[renterId]||0)+1;else unassigned++;}return Object.freeze({records:rows.length,totalCents,unassignedTransactions:unassigned,renterTransactionCounts:Object.freeze(byRenter),page:Number(payload.summary?.page||page),moreRecords:Boolean(payload.summary?.more_records),startingBalanceCents:cents(payload.summary?.starting_balance),endingBalanceCents:cents(payload.summary?.ending_balance)});}}
export function createRentecApiClient(options={}){return new RentecApiClient({apiKey:process.env.RENTEC_API_KEY,...options});}
