import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";const sql=readFileSync("supabase/migrations/20260813003400_add_rental_contractor_tax_ledger.sql","utf8");describe("contractor tax ledger",()=>{it("links payments to contractor property work and invoice evidence",()=>{expect(sql).toContain("rental_contractor_payments");expect(sql).toContain("work_order_id");expect(sql).toContain("invoice_document_id")});it("creates an accountant-controlled review instead of deciding eligibility",()=>{expect(sql).toContain("'needs_review'");expect(sql).toContain("'accountant_reviewed'");expect(sql).toContain("'filing_required'")});it("tracks preparation filing and corrections",()=>{expect(sql).toContain("'prepared','filed','corrected'");expect(sql).toContain("filing_reference")});});
