import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";const sql=readFileSync("supabase/migrations/20260813003200_add_renters_insurance_evidence_workflow.sql","utf8");describe("insurance evidence",()=>{it("uses private bounded evidence storage",()=>{expect(sql).toContain("'rental-insurance-evidence'");expect(sql).toContain("false,10485760")});it("requires evidence owned by the submitting tenant",()=>{expect(sql).toContain("e.tenant_id=t.id");expect(sql).toContain("e.lease_id=p_lease_id")});it("requires owner review before verification",()=>{expect(sql).toContain("'pending_verification'");expect(sql).toContain("p_owner_id<>auth.uid()::text")});it("queues idempotent 30 and 7 day reminders",()=>{expect(sql).toContain("p.expiration_date-p_as_of in(30,7)");expect(sql).toContain("on conflict(event_key)do nothing")});});
