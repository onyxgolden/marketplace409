import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";const sql=readFileSync("supabase/migrations/20260813003100_ingest_rental_stripe_settlements.sql","utf8");describe("settlement ingestion",()=>{it("matches PaymentIntent and validates fee arithmetic",()=>{expect(sql).toContain("provider_payment_id=p_payment_intent_id");expect(sql).toContain("p_gross_amount_cents-p_fee_amount_cents<>p_net_amount_cents")});it("is idempotent by balance transaction",()=>expect(sql).toContain("on conflict(provider,provider_balance_transaction_id)do update"));it("marks only Stripe-listed payout transactions",()=>expect(sql).toContain("provider_balance_transaction_id=any(p_balance_transaction_ids)"));});
