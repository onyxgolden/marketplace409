import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";const sql=readFileSync("supabase/migrations/20260813002900_add_rental_reminder_retry_controls.sql","utf8");describe("rental reminders",()=>{it("adds scheduled reminders with bounded retries",()=>{expect(sql).toContain("scheduled_for");expect(sql).toContain("max_attempts smallint");expect(sql).toContain("p_max_attempts not between 1 and 5")});it("only queues open owner charges",()=>{expect(sql).toContain("p_owner_id<>auth.uid()::text");expect(sql).toContain("status in('scheduled','due','partially_paid','overdue')")});it("supports cancellation",()=>expect(sql).toContain("cancel_rental_notification"));});
