import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";const sql=readFileSync("supabase/migrations/20260813003500_add_rental_support_cases.sql","utf8");describe("rental support cases",()=>{it("covers payment refund dispute and access incidents",()=>{expect(sql).toContain("'failed_payment'");expect(sql).toContain("'refund_request'");expect(sql).toContain("'payment_dispute'");expect(sql).toContain("'portal_access'")});it("keeps owner notes private from tenants",()=>{expect(sql).toContain("private_owner_note");expect(sql).toContain("public_note is not null")});it("preserves append-only resolution history",()=>{expect(sql).toContain("rental_support_case_events");expect(sql).not.toMatch(/delete\s+from\s+rental_support_case_events/i)});});
