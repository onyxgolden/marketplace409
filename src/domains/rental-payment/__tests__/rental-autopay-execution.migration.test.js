import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";const sql=readFileSync("supabase/migrations/20260813003600_activate_rental_autopay_execution.sql","utf8");describe("autopay execution",()=>{it("activates only from a succeeded tenant payment",()=>{expect(sql).toContain("p.status<>'succeeded'");expect(sql).toContain("status='setup_required'")});it("stores Stripe payment method and mandate evidence",()=>{expect(sql).toContain("provider_payment_method_id=p_payment_method_id");expect(sql).toContain("provider_mandate_id=p_mandate_id")});it("prevents duplicate charge attempts",()=>expect(sql).toContain("unique(owner_id,enrollment_id,charge_id)"));});
