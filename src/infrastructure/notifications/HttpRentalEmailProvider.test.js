import{describe,expect,it,vi}from"vitest";import{createHttpRentalEmailProvider}from"./HttpRentalEmailProvider";describe("HTTP rental email provider",()=>{it("requires explicit provider configuration",()=>{expect(()=>createHttpRentalEmailProvider({},vi.fn())).toThrow("RENTAL_EMAIL_PROVIDER_URL")});it("sends an idempotent plain-text message",async()=>{const request=vi.fn(async()=>new Response(JSON.stringify({id:"msg_1"}),{status:200})),provider=createHttpRentalEmailProvider({RENTAL_EMAIL_PROVIDER_URL:"https://provider.test/send",RENTAL_EMAIL_PROVIDER_TOKEN:"token"},request);expect(await provider.send({id:"n1",ownerId:"o1",senderName:"FORGE",senderEmail:"rent@example.com",recipient:"tenant@example.com",subject:"Receipt",bodyText:"Paid"})).toEqual({messageId:"msg_1"});expect(request.mock.calls[0][1].headers["idempotency-key"]).toBe("n1")})});
