"use client";import{useEffect,useState}from"react";
export default function RentalAutopayPanel(){const[items,setItems]=useState([]);const[error,setError]=useState("");useEffect(()=>{fetch("/api/rental").then(async r=>{const b=await r.json();if(!r.ok)throw new Error(b.error);setItems(b.autopayEnrollments||[])}).catch(e=>setError(e.message))},[]);return <section className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm dark:border-slate-700 dark:bg-slate-900"><p className="text-xs font-black uppercase tracking-[0.2em] text-sky-700 dark:text-sky-400">Autopay</p><h2 className="mt-1 text-3xl font-black tracking-tight text-slate-950 dark:text-white">Tenant authorizations</h2><p className="mt-2 text-sm text-slate-600 dark:text-slate-400">Consent alone never activates a debit. “Setup required” means Stripe has not yet supplied both a reusable payment method and mandate.</p>{error?<p role="alert" className="mt-4 rounded-xl bg-red-50 p-3 text-sm font-bold text-red-800 dark:bg-red-950/40 dark:text-red-300">{error}</p>:null}<div className="mt-5 space-y-3">{items.length?items.map(item=><article key={item.id} className="rounded-xl border border-slate-200 p-4 dark:border-slate-700"><strong className="text-slate-950 dark:text-white">{item.status.replaceAll("_"," ")}</strong><p className="text-sm text-slate-600 dark:text-slate-400">Lease {item.lease_id} · {item.payment_method_type.replaceAll("_"," ")} · day {item.charge_day}</p><p className="mt-1 text-xs text-slate-500 dark:text-slate-400">Consented {new Date(item.consented_at).toLocaleString()}{item.cancelled_at?` · Cancelled ${new Date(item.cancelled_at).toLocaleString()}`:""}</p></article>):<p className="rounded-xl bg-slate-50 p-4 text-sm text-slate-600 dark:bg-slate-950/40 dark:text-slate-400">No tenant autopay authorizations recorded.</p>}</div></section>}
