"use client";import{useCallback,useEffect,useState}from"react";import RentalRecordBrowser from"./RentalRecordBrowser";const label=value=>value?.replaceAll("_"," ")||"—";
export default function RentalCommunicationsPanel({initialData=null}){const[data,setData]=useState(initialData||{notifications:[],charges:[]}),[selectedId,setSelectedId]=useState(""),[showQueue,setShowQueue]=useState(false),[error,setError]=useState(""),[message,setMessage]=useState("");const load=useCallback(()=>fetch("/api/rental").then(async response=>{const body=await response.json();if(!response.ok)throw new Error(body.error);setData({notifications:body.notifications||[],charges:body.openCharges||[]})}),[]);useEffect(()=>{if(!initialData)load().catch(reason=>setError(reason.message))},[initialData,load]);const activeId=data.notifications.some(item=>item.id===selectedId)?selectedId:data.notifications[0]?.id||"",selected=data.notifications.find(item=>item.id===activeId);
async function post(payload,text){setError("");const response=await fetch("/api/rental",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(payload)}),body=await response.json();if(!response.ok)throw new Error(body.error);setMessage(text);await load()}
async function queue(event){event.preventDefault();const element=event.currentTarget,form=new FormData(element);try{await post({operation:"queue-rent-reminder",chargeId:form.get("chargeId"),notificationType:form.get("notificationType"),scheduledFor:new Date(form.get("scheduledFor")).toISOString(),maxAttempts:Number(form.get("maxAttempts"))},"Reminder queued for owner-controlled delivery.");element.reset();setShowQueue(false)}catch(reason){setError(reason.message)}}
return <section className="rounded-2xl border bg-white p-6 shadow-sm"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="text-sm font-bold uppercase tracking-widest text-amber-700">Communications</p><h2 className="mt-2 text-2xl font-black">Notification outbox</h2><p className="mt-2 text-slate-600">Review queued messages and bounded retries. Email sending remains disabled.</p></div><button type="button" onClick={()=>setShowQueue(value=>!value)} className="rounded-xl bg-slate-950 px-4 py-2 font-bold text-white">{showQueue?"Cancel reminder":"Queue reminder"}</button></div>{error?<p role="alert" className="mt-4 rounded-xl bg-red-50 p-3 text-red-800">{error}</p>:null}{message?<p role="status" className="mt-4 rounded-xl bg-emerald-50 p-3 text-emerald-800">{message}</p>:null}{showQueue?<form aria-label="Queue rent reminder" onSubmit={queue} className="mt-6 grid gap-3 rounded-2xl border bg-slate-50 p-5 md:grid-cols-2"><select name="chargeId" required className="rounded-xl border p-3"><option value="">Select open charge</option>{data.charges.map(item=><option key={item.id} value={item.id}>{item.period} · due {item.due_date}</option>)}</select><select name="notificationType" className="rounded-xl border p-3"><option value="rent_reminder">Upcoming reminder</option><option value="balance_overdue">Overdue balance</option></select><input aria-label="Schedule time" name="scheduledFor" type="datetime-local" required className="rounded-xl border p-3"/><label className="text-sm font-bold">Maximum attempts<input name="maxAttempts" type="number" min="1" max="5" defaultValue="3" className="mt-1 w-full rounded-xl border p-3 font-normal"/></label><button className="rounded-xl bg-slate-950 px-4 py-3 font-bold text-white md:col-span-2">Queue reminder for review</button></form>:null}<RentalRecordBrowser title="Communications" records={data.notifications} selectedId={activeId} onSelect={setSelectedId} getTitle={item=>item.subject} getSubtitle={item=>`${label(item.status)} · ${label(item.notification_type)}`} emptyMessage="No notifications queued.">{!selected?<p className="text-sm text-slate-500">No tenant communication requires review.</p>:<div><p className="text-xs font-bold uppercase tracking-widest text-slate-500">Selected communication</p><h3 className="mt-2 text-xl font-black">{selected.subject}</h3><dl className="mt-5 grid gap-4 sm:grid-cols-2"><Fact term="Status" value={label(selected.status)}/><Fact term="Type" value={label(selected.notification_type)}/><Fact term="Recipient" value={selected.recipient}/><Fact term="Scheduled" value={selected.scheduled_for?new Date(selected.scheduled_for).toLocaleString():"—"}/><Fact term="Attempts" value={`${selected.attempt_count||0} of ${selected.max_attempts||0}`}/><Fact term="Channel" value={label(selected.channel||"email")}/></dl><div className="mt-5 rounded-xl bg-slate-50 p-4 text-sm">{selected.body_text}</div>{selected.failure_message?<p className="mt-3 rounded-xl bg-red-50 p-3 text-sm text-red-800">{selected.failure_message}</p>:null}{["queued","failed"].includes(selected.status)?<button onClick={()=>post({operation:"cancel-rent-notification",notificationId:selected.id},"Queued communication cancelled.").catch(reason=>setError(reason.message))} className="mt-5 rounded-xl border px-4 py-2 font-bold">Cancel communication</button>:null}</div>}</RentalRecordBrowser><p className="mt-5 rounded-xl bg-amber-50 p-4 text-sm text-amber-900">Nothing is sent until the approved email provider, sender domain, and delivery control are activated.</p></section>}
function Fact({term,value}){return <div><dt className="text-xs font-bold uppercase text-slate-500">{term}</dt><dd className="mt-1 break-words font-bold capitalize">{value||"—"}</dd></div>}
