"use client";
import { useEffect, useState } from "react";
import { filterRentalRecordsByContext, rentalContextLeaseIds } from "./filterRentalRecordsByContext";
import RentalPaymentsPanel from "./RentalPaymentsPanel";import RentalLeasePanel from "./RentalLeasePanel";import RentalMaintenancePanel from "./RentalMaintenancePanel";import RentalInspectionsPanel from "./RentalInspectionsPanel";import RentalDocumentsPanel from "./RentalDocumentsPanel";import RentalCommunicationsPanel from "./RentalCommunicationsPanel";
export function buildContextualRentalData(data, context) {const leaseIds=rentalContextLeaseIds(data,context),keep=records=>filterRentalRecordsByContext(records,data,context);return{...data,leases:keep(data.leases),schedules:keep(data.schedules),openCharges:keep(data.openCharges),payments:keep(data.payments),maintenanceRequests:keep(data.maintenanceRequests),inspections:keep(data.inspections),notifications:keep(data.notifications),documents:keep(data.documents),leaseMemberships:(data.leaseMemberships||[]).filter(item=>leaseIds.has(item.lease_id))}}
export default function RentalContextualSurface({surfaceId,recordContext}){const[data,setData]=useState(null),[error,setError]=useState("");useEffect(()=>{Promise.all([fetch("/api/rental"),...(surfaceId==="documents"||surfaceId==="inspections"?[fetch("/api/rental/documents")]:[])]).then(async responses=>{const bodies=await Promise.all(responses.map(response=>response.json()));if(!responses[0].ok)throw new Error(bodies[0].error);if(responses[1]&&!responses[1].ok)throw new Error(bodies[1].error);setData(buildContextualRentalData({...bodies[0],documents:bodies[1]?.documents||[]},recordContext))}).catch(reason=>setError(reason.message))},[surfaceId,recordContext.recordType,recordContext.recordId]);if(error)return <p role="alert" className="rounded-xl bg-red-50 p-4 text-red-800">{error}</p>;if(!data)return <p role="status" className="rounded-xl bg-white p-6">Loading records for {recordContext.recordLabel}…</p>;const surfaces={charges:<RentalPaymentsPanel initialData={data}/>,leases:<RentalLeasePanel initialSetup={data} loadOnMount={false}/>,maintenance:<RentalMaintenancePanel initialData={data}/>,inspections:<RentalInspectionsPanel initialData={data}/>,documents:<RentalDocumentsPanel initialData={data}/>,communications:<RentalCommunicationsPanel initialData={{notifications:data.notifications,charges:data.openCharges}}/>};return surfaces[surfaceId]||null}
