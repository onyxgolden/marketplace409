"use client";import{useState}from"react";
export default function TenantAutopayPanel({rentals,onChanged}){const[busy,setBusy]=useState(false);const[message,setMessage]=useState("");const current=rentals.flatMap(r=>r.autopayEnrollments||[]).find(e=>["setup_required","active","paused"].includes(e.status));
async function submit(payload){setBusy(true);setMessage("");try{const response=await fetch("/api/rental/portal",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(payload)});const body=await response.json();if(!response.ok)throw new Error(body.error);setMessage(payload.operation==="cancel-autopay"?"Autopay enrollment cancelled.":"Consent recorded. Payment-method authorization must still be completed before automatic charges can begin.");await onChanged();}catch(error){setMessage(error.message);}finally{setBusy(false);}}
return <section className="rounded-2xl border bg-white p-6 shadow-sm"><p className="text-sm font-bold uppercase tracking-widest text-amber-700">Automatic payments</p><h2 className="mt-2 text-xl font-black">Autopay controls</h2>{current?<><p className="mt-3 text-sm">Status: <strong>{current.status.replaceAll("_"," ")}</strong> · charge day {current.chargeDay}</p><p className="mt-2 text-sm text-slate-600">{current.status==="setup_required"?"No automatic debit can occur yet. Stripe payment-method and mandate setup is still required.":"You may cancel future automatic payments at any time."}</p><button disabled={busy} onClick={()=>submit({operation:"cancel-autopay",enrollmentId:current.id,reason:"Cancelled by tenant in portal"})} className="mt-4 rounded-xl border px-4 py-2 font-bold">Cancel autopay</button></>:<form className="mt-4 grid gap-4" onSubmit={event=>{event.preventDefault();const f=new FormData(event.currentTarget);submit({operation:"request-autopay",leaseId:f.get("leaseId"),paymentMethodType:f.get("paymentMethodType"),chargeDay:Number(f.get("chargeDay")),reminderDaysBefore:Number(f.get("reminderDaysBefore")),consentConfirmed:f.get("consentConfirmed")==="on"});}}><label className="text-sm font-bold">Lease<select name="leaseId" required className="mt-1 w-full rounded-xl border p-3 font-normal">{rentals.map(r=><option key={r.lease.id} value={r.lease.id}>{r.unit?.label||"Rental home"}</option>)}</select></label><label className="text-sm font-bold">Payment method<select name="paymentMethodType" className="mt-1 w-full rounded-xl border p-3 font-normal"><option value="us_bank_account">US bank account</option><option value="card">Card</option></select></label><div className="grid grid-cols-2 gap-3"><label className="text-sm font-bold">Charge day<input name="chargeDay" type="number" min="1" max="28" defaultValue="1" className="mt-1 w-full rounded-xl border p-3 font-normal"/></label><label className="text-sm font-bold">Reminder days before<input name="reminderDaysBefore" type="number" min="0" max="14" defaultValue="3" className="mt-1 w-full rounded-xl border p-3 font-normal"/></label></div><label className="flex gap-3 rounded-xl bg-amber-50 p-4 text-sm"><input name="consentConfirmed" type="checkbox" required/><span>I authorize recurring rent payments under the schedule shown here. I understand enrollment is not active until Stripe securely verifies my payment method and mandate, and I may cancel future payments.</span></label><button disabled={busy} className="rounded-xl bg-slate-950 px-5 py-3 font-bold text-white disabled:opacity-50">{busy?"Saving…":"Continue autopay setup"}</button></form>}{message?<p role="status" className="mt-4 rounded-xl bg-blue-50 p-3 text-sm text-blue-900">{message}</p>:null}</section>}
