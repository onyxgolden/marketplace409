"use client";import{useMemo,useState}from"react";import{Elements}from"@stripe/react-stripe-js";import{loadStripe}from"@stripe/stripe-js";import TenantPaymentForm from"./TenantPaymentForm";
const KEY=process.env.NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY;
export default function PrivateFinancingBorrowerPayment({accountId,regularScheduledPaymentCents,onCancel}){const[amount,setAmount]=useState((regularScheduledPaymentCents/100).toFixed(2)),[session,setSession]=useState(null),[error,setError]=useState(""),[busy,setBusy]=useState(false);const stripe=useMemo(()=>session&&KEY?loadStripe(KEY,{stripeAccount:session.connectedAccountId}):null,[session]);async function start(){const cents=Math.round(Number(amount)*100);if(!Number.isSafeInteger(cents)||cents<=0){setError("Enter a valid payment amount.");return;}setBusy(true);setError("");const response=await fetch("/api/private-financing/portal/payment-session",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({accountId,amountCents:cents})});const payload=await response.json();setBusy(false);if(!response.ok){setError(payload.error);return;}setSession(payload);}if(session)return <Elements stripe={stripe} options={{clientSecret:session.clientSecret,appearance:{theme:"stripe"}}}><TenantPaymentForm returnUrl={session.returnUrl} amountLabel={`$${(session.amountCents/100).toFixed(2)}`} dueDate="Current financing account" chargeLabel="Financing" onCancel={()=>setSession(null)}/></Elements>;
return <div className="mt-6 rounded-2xl border border-amber-300 p-5"><h3 className="text-lg font-black">Make a payment</h3><p className="mt-1 text-sm">Pay securely by bank account or eligible card. FORGE charges no platform fee. The seller credits the actual Stripe processing fee back to your principal after settlement.</p><label className="mt-4 block text-sm font-bold">Payment amount<input type="number" min="0.01" step="0.01" value={amount} onChange={e=>setAmount(e.target.value)} className="mt-1 block w-full rounded-lg border p-3 text-slate-950"/></label><button onClick={start} disabled={busy} className="mt-3 rounded-xl bg-amber-500 px-5 py-3 font-black text-slate-950 disabled:opacity-50">{busy?"Starting…":"Continue to secure payment"}</button><button onClick={onCancel} className="ml-3 mt-3 rounded-xl border px-5 py-3 font-bold">Cancel</button>{error?<p role="alert" className="mt-3 text-sm font-bold text-red-700">{error}</p>:null}</div>}
