"use client";import{useCallback,useEffect,useState}from"react";import RentalRecordBrowser from"./RentalRecordBrowser";const money=new Intl.NumberFormat("en-US",{style:"currency",currency:"USD"}),label=value=>value?.replaceAll("_"," ")||"—";
export const depositBalance=(deposit,transactions)=>transactions.filter(item=>item.deposit_id===deposit.id).reduce((sum,item)=>sum+(["received","adjustment_increase"].includes(item.transaction_type)?1:-1)*Number(item.amount_cents),0);
export default function RentalDepositsPanel({initialData=null}){const[data,setData]=useState(initialData||{deposits:[],transactions:[],schedules:[],tenants:[]}),[selectedId,setSelectedId]=useState(""),[showAdd,setShowAdd]=useState(false),[showTransaction,setShowTransaction]=useState(false),[error,setError]=useState(""),[message,setMessage]=useState("");const load=useCallback(()=>fetch("/api/rental").then(async response=>{const body=await response.json();if(!response.ok)throw new Error(body.error);setData({deposits:body.deposits||[],transactions:body.depositTransactions||[],schedules:body.schedules||[],tenants:body.tenants||[]})}),[]);useEffect(()=>{if(!initialData)load().catch(reason=>setError(reason.message))},[initialData,load]);const activeId=data.deposits.some(item=>item.id===selectedId)?selectedId:data.deposits[0]?.id||"",selected=data.deposits.find(item=>item.id===activeId),transactions=data.transactions.filter(item=>item.deposit_id===activeId);
async function post(payload){setError("");setMessage("");const response=await fetch("/api/rental",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(payload)}),body=await response.json();if(!response.ok)throw new Error(body.error);await load()}
async function save(event){event.preventDefault();const element=event.currentTarget,form=new FormData(element);try{await post({operation:"save-security-deposit",deposit:{leaseId:form.get("leaseId"),tenantId:form.get("tenantId"),requiredAmountCents:Math.round(Number(form.get("amount"))*100),jurisdictionCode:form.get("jurisdictionCode")}});element.reset();setShowAdd(false);setMessage("Security-deposit obligation saved.")}catch(reason){setError(reason.message)}}
async function transact(event){event.preventDefault();const element=event.currentTarget,form=new FormData(element);try{await post({operation:"record-security-deposit-transaction",transaction:{depositId:activeId,transactionType:form.get("transactionType"),amountCents:Math.round(Number(form.get("amount"))*100),occurredAt:new Date(`${form.get("date")}T12:00:00`).toISOString(),description:form.get("description")}});element.reset();setShowTransaction(false);setMessage("Deposit transaction recorded.")}catch(reason){setError(reason.message)}}
return <section className="rounded-2xl border bg-white p-6 shadow-sm"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="text-sm font-bold uppercase tracking-widest text-amber-700">Liability accounting</p><h2 className="mt-2 text-2xl font-black">Security deposits</h2><p className="mt-2 text-slate-600">Deposits remain tenant liabilities and are never treated as rent or NOI.</p></div><button type="button" onClick={()=>setShowAdd(value=>!value)} className="rounded-xl bg-slate-950 px-4 py-2 font-bold text-white">{showAdd?"Cancel setup":"Add deposit requirement"}</button></div>{error?<p role="alert" className="mt-4 rounded-xl bg-red-50 p-3 text-red-800">{error}</p>:null}{message?<p role="status" className="mt-4 rounded-xl bg-emerald-50 p-3 text-emerald-800">{message}</p>:null}
{showAdd?<form aria-label="Add deposit requirement" onSubmit={save} className="mt-6 grid gap-3 rounded-2xl border bg-slate-50 p-5 md:grid-cols-4"><select name="leaseId" required className="rounded-lg border p-3"><option value="">Lease</option>{data.schedules.map(item=><option key={item.id} value={item.lease_id}>{item.lease_id}</option>)}</select><select name="tenantId" required className="rounded-lg border p-3"><option value="">Tenant</option>{data.tenants.map(item=><option key={item.id} value={item.id}>{item.display_name}</option>)}</select><input name="amount" type="number" min="0" step="0.01" required placeholder="Required amount" className="rounded-lg border p-3"/><input name="jurisdictionCode" placeholder="Jurisdiction (example TX)" className="rounded-lg border p-3"/><button className="rounded-lg bg-slate-950 px-4 py-3 font-bold text-white md:col-span-4">Save deposit requirement</button></form>:null}
<RentalRecordBrowser title="Deposit obligations" records={data.deposits} selectedId={activeId} onSelect={id=>{setSelectedId(id);setShowTransaction(false)}} getTitle={item=>money.format(Number(item.required_amount_cents)/100)} getSubtitle={item=>`${label(item.status)} · ${item.jurisdiction_code||"jurisdiction not recorded"}`} emptyMessage="No deposit obligation recorded.">{!selected?<p className="text-sm text-slate-500">Add the lease deposit requirement before recording money received.</p>:<div><p className="text-xs font-bold uppercase tracking-widest text-slate-500">Selected deposit</p><div className="mt-2 flex flex-wrap justify-between gap-4"><div><h3 className="text-xl font-black">{money.format(Number(selected.required_amount_cents)/100)} required</h3><p className="mt-1 text-sm capitalize">{label(selected.status)} · {selected.jurisdiction_code||"Jurisdiction not recorded"}</p></div><div><p className="text-xs font-bold uppercase text-slate-500">Held balance</p><p className="text-2xl font-black">{money.format(depositBalance(selected,data.transactions)/100)}</p></div></div><button type="button" onClick={()=>setShowTransaction(value=>!value)} className="mt-5 rounded-xl bg-amber-500 px-4 py-2 font-black">{showTransaction?"Cancel transaction":"Record deposit transaction"}</button>{showTransaction?<form aria-label="Record deposit transaction" onSubmit={transact} className="mt-4 grid gap-3 rounded-xl bg-slate-50 p-4 md:grid-cols-2"><select name="transactionType" required className="rounded-lg border p-3"><option value="received">Deposit received</option><option value="deduction">Documented deduction</option><option value="refunded">Refund to tenant</option><option value="adjustment_increase">Increase adjustment</option><option value="adjustment_decrease">Decrease adjustment</option></select><input name="amount" type="number" min="0.01" step="0.01" required placeholder="Amount" className="rounded-lg border p-3"/><input name="date" type="date" required className="rounded-lg border p-3"/><input name="description" required placeholder="Reason and evidence reference" className="rounded-lg border p-3"/><button className="rounded-lg bg-slate-950 px-4 py-3 font-bold text-white md:col-span-2">Record transaction</button></form>:null}<div className="mt-6 border-t pt-4"><h4 className="font-black">Transaction history</h4>{transactions.length?transactions.map(item=><p key={item.id} className="mt-2 text-sm capitalize"><strong>{label(item.transaction_type)}</strong> · {money.format(Number(item.amount_cents)/100)} · {item.occurred_at?new Date(item.occurred_at).toLocaleDateString():"—"}</p>):<p className="mt-2 text-sm text-slate-500">No deposit transactions recorded.</p>}</div></div>}</RentalRecordBrowser><p className="mt-5 rounded-xl bg-amber-50 p-4 text-sm text-amber-900">FORGE records dates and evidence but does not determine allowable deductions or legal deadlines.</p></section>}
