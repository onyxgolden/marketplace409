import{describe,expect,it}from"vitest";import{renderToStaticMarkup}from"react-dom/server";import RentecMigrationPanel from"./RentecMigrationPanel.jsx";describe("RentecMigrationPanel",()=>{it("makes API inspection primary and CSV secondary",()=>{const html=renderToStaticMarkup(<RentecMigrationPanel/>);expect(html).toContain("Inspect Rentec account");expect(html).toContain("cannot write Rentec or FORGE records");expect(html).toContain("CSV backup and reconciliation");expect(html).not.toContain("Commit import");});});
