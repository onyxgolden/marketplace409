import{describe,expect,it}from"vitest";import{renderToStaticMarkup}from"react-dom/server";import RentecMigrationPanel from"./RentecMigrationPanel.jsx";describe("RentecMigrationPanel",()=>{it("is explicitly preview-only",()=>{const html=renderToStaticMarkup(<RentecMigrationPanel/>);expect(html).toContain("Import from Rentec Direct");expect(html).toContain("tenant-list and tenant-ledger CSV exports");expect(html).toContain("Preview only: this screen cannot write rental records");expect(html).not.toContain("Commit import");});});
