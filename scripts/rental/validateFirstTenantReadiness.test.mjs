import{describe,expect,it}from"vitest";import{inspectFirstTenantReadiness,RENTAL_MIGRATIONS}from"./validateFirstTenantReadiness.mjs";
describe("rental first-tenant readiness",()=>{it("verifies the complete ordered migration chain",()=>{const result=inspectFirstTenantReadiness({root:process.cwd(),env:{},schemaOnly:true});expect(result.ready).toBe(true);expect(result.migrationCount).toBe(20);expect(RENTAL_MIGRATIONS[0]).toMatch(/^20260812001800/);expect(RENTAL_MIGRATIONS.at(-1)).toMatch(/^20260813003700/)});it("fails closed without core server configuration",()=>{const result=inspectFirstTenantReadiness({root:process.cwd(),env:{}});expect(result.ready).toBe(false);expect(result.blockers).toContain("Missing core server configuration: SUPABASE_SERVICE_ROLE_KEY");expect(result.warnings.join(" ")).toContain("Email delivery must remain paused")});it("reports configured infrastructure as ready while activation remains a human decision",()=>{const env={NEXT_PUBLIC_SUPABASE_URL:"url",NEXT_PUBLIC_SUPABASE_ANON_KEY:"anon",SUPABASE_SERVICE_ROLE_KEY:"service",STRIPE_SECRET_KEY:"stripe",STRIPE_CONNECT_WEBHOOK_SECRET:"webhook",RENTAL_AUTOPAY_EXECUTION_SECRET:"autopay",RENTAL_NOTIFICATION_DELIVERY_SECRET:"delivery",RENTAL_EMAIL_PROVIDER_URL:"provider",RENTAL_EMAIL_PROVIDER_TOKEN:"token"};const result=inspectFirstTenantReadiness({root:process.cwd(),env});expect(result).toMatchObject({ready:true,blockers:[],warnings:[]})})});
